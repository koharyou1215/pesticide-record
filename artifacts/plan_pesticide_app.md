# Pesticide App Plan

- Add a standalone React 19 + TypeScript + Vite app at the repository root
- Keep existing Python files untouched
- Implement calculation, pesticide management, and usage records with localStorage
- Verify with `npm run build`
- Run a local dev server after implementation
